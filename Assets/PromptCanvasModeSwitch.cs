using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PromptCanvasModeSwitch : MonoBehaviour
{

    public GameObject ThePhoneScreen;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void PhoneMode()
    {
        print("UI turns into a phone implementation");
    }

    public void HeadsetMode()
    {
        print("UI turns into a headset mode");
    }
}
